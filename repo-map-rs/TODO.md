# extract this to own repo

# compare aider outputs to this for the exact same repo

# how does file update mode work? I see there are some CLI flags?

I think aider repo map is cached in file system, or so?

# update all treesitter versions in cargo to latest version

Maybe? Maybe, some older versions need to be used though?
